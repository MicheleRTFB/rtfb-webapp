# Inserisci qui le tue credenziali di intervals.icu
# Puoi trovarle in Settings > API Access
VITE_INTERVALS_API_KEY=your_api_key_here

# ========================================
# N8N WEBHOOK CONFIGURATION
# ========================================
# Configura gli URL dei tuoi webhook n8n per integrazioni automatiche
# Lascia vuoto se non usi n8n

# Webhook chiamato quando viene creato un nuovo allenamento
# Esempio di utilizzo: inviare email, aggiungere a Google Calendar, notificare su Slack
VITE_N8N_WEBHOOK_WORKOUT_CREATED=https://your-n8n-instance.com/webhook/workout-created

# Webhook chiamato quando un allenamento viene completato
# Esempio di utilizzo: aggiornare statistiche, congratulazioni automatiche, analisi performance
VITE_N8N_WEBHOOK_WORKOUT_COMPLETED=https://your-n8n-instance.com/webhook/workout-completed

# Webhook chiamato quando le statistiche vengono aggiornate
# Esempio di utilizzo: report settimanali, dashboard updates, alerts su fitness/forma
VITE_N8N_WEBHOOK_STATS_UPDATED=https://your-n8n-instance.com/webhook/stats-updated

# Webhook chiamato quando viene registrato un nuovo atleta
# Esempio di utilizzo: email di benvenuto, setup iniziale, notifica coach
VITE_N8N_WEBHOOK_ATHLETE_REGISTERED=https://your-n8n-instance.com/webhook/athlete-registered

# ========================================
# ESEMPI DI FLUSSI N8N
# ========================================
#
# 1. NOTIFICA ALLENAMENTO CREATO:
#    Webhook Trigger → Gmail (email atleta) → Google Calendar (aggiungi evento) → Slack (notifica coach)
#
# 2. ALLENAMENTO COMPLETATO:
#    Webhook Trigger → Google Sheets (aggiorna statistiche) → Telegram (congratulazioni) → Airtable (log)
#
# 3. REPORT SETTIMANALE:
#    Webhook Stats → Function (calcola metriche) → Gmail (invia report) → Notion (aggiorna dashboard)
#
# 4. ALERT FITNESS:
#    Webhook Stats → IF (fitness < soglia) → Telegram (alert coach) → Trello (crea task)
#
# ========================================
# CONFIGURAZIONE INCOMING WEBHOOKS
# ========================================
# Se vuoi ricevere dati da n8n nella tua app, dovrai creare un endpoint API
# Esempio: POST /api/webhooks/n8n
# n8n può quindi inviare dati per importare workout, sincronizzare dati esterni, etc.
